import { NextRequest, NextResponse } from "next/server";
export declare function handleAuthenticationInRequest(request: NextRequest, verbose: boolean): Promise<{
    kind: "redirect";
    response: NextResponse;
} | {
    kind: "refreshTokens";
    refreshTokens: {
        token: string;
        refreshToken: string;
    } | null | undefined;
}>;
